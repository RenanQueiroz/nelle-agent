# Dependencies
node_modules/

# Build output
dist/
build/
coverage/
playwright-report/
test-results/
.playwright-mcp/

# Local planning / scratch docs (not shared; AGENTS.md is the committed source of truth)
plans/

# Local app/runtime data. Anchored to the repo root so an unanchored name like
# `models/` doesn't also swallow nested dirs -- e.g. the Flutter client's
# generated `apps/client/lib/src/api/generated/models/`.
.nelle/
.nelle-e2e/
/data/
/logs/
/llamacpp/
/llamacpp-src/
/models/

# Environment and secrets
.env
.env.*
!.env.example

# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
.nelle-device/
